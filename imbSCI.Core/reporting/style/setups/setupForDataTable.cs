namespace imbSCI.Core.reporting.style.setups
{

    //public class setupForDataTable : setupForBlockBase
    //{

    //    public setupForDataTable(DataTable table, cursor cur)
    //    {
    //        selectRangeArea area = new selectRangeArea(cur.x, cur.y, table.Columns.Count + cur.x, table.Rows.Count + cur.y);

    //        area.expand(textCursorZoneCorner.Top, 1, doInsertHeader, doInsertColumnHeading);
    //        area.expand(textCursorZoneCorner.Bottom, 1, doInsertColumnFooter, doInsertFooter);
    //        area.expand(textCursorZoneCorner.Left, 1, doInsertRowId);
    //        area.expand(textCursorZoneCorner.Right, 1, doInsertMinorMajor);

    //    }


    //    public Boolean doInsertRowId
    //    {
    //        get
    //        {
    //            return headFootFlags.HasFlag(cursorVariatorHeadFootFlags.doLeftZone);
    //        }
    //    }

    //    public Boolean doInsertMinorMajor
    //    {
    //        get
    //        {
    //            return headFootFlags.HasFlag(cursorVariatorHeadFootFlags.doRightZone) || headFootFlags.HasFlag(cursorVariatorHeadFootFlags.addRowNumberOnMajor) || headFootFlags.HasFlag(cursorVariatorHeadFootFlags.addRowNumberOnMinor);
    //        }
    //    }

    //    public Boolean doInsertColumnHeading
    //    {
    //        get
    //        {
    //            return headFootFlags.HasFlag(cursorVariatorHeadFootFlags.doHeadExtenedZone);
    //        }
    //    }

    //    public Boolean doInsertColumnFooter
    //    {
    //        get
    //        {
    //            return headFootFlags.HasFlag(cursorVariatorHeadFootFlags.addColumnDescForFoot) || headFootFlags.HasFlag(cursorVariatorHeadFootFlags.addColumnTypeForFoot);
    //        }
    //    }



    //}
}
